(() => {

    // let numeroTest = session.getVariable("numero_test");
    // let stringaRichiesta = "";
    //if (numeroTest == 1) { 
        // stringaRichiesta = getEntryPointSysint(numeroTest)+"/api/sysint/home/getByPhone/"+session.getVariable("caller_id_number")
    //} else {
    //    stringaRichiesta = 'https://'+getUser(numeroTest)+':'+getPwd(numeroTest)+'@'+getEntryPointOcto(numeroTest)+'/custcare/v1.0/identificaCliente/'+session.getVariable("caller_id_number");
    //}
    

    
    
    log.info('Check user')

    
    // log.info('RICHIESTA ' + stringaRichiesta)

    // let response = request.get(stringaRichiesta)

    // log.warning(response)


    // let a = request.get(stringaRichiesta);

    // log.info('Result code ' + a.code + " - result description " + a.data)



    // let parsedJSON = JSON.parse(a.data)

    // // let numeroUtenze = 1;
    // if (a.code == '200') {
    //     numeroUtenze = parsedJSON.length;    
    //     if (parsedJSON.length == 1) {
    //         //Un caso solo
    //         //Valido
    //         session.setVariable("voucher",parsedJSON[0]['ID Voucher']);
    //         session.setVariable("user",parsedJSON[0]['USER ID']);
    //         session.setVariable("customerName",parsedJSON[0]['Ragione Sociale Cliente']);
    //         session.setVariable("contractType",parsedJSON[0]['ID Contract Type']);
    //         session.setVariable("owner",parsedJSON[0]['OWNER COMPANY']);
    //         session.setVariable("mobileNumber",parsedJSON[0]['NUMERO MOBILE']);
    //     } 
        
    
    // } else {
    //     numeroUtenze = 0;
    // }

    // session.setVariable("userIdentification",a.code);
    // return true;
    

    

})()